services:
  scorer:
    build: .
    command: ["jobtrail-ai-scorer", "score", "--config", "/config/config.yaml"]
    volumes:
      - ${SCORER_CONFIG_PATH:-./config.example.yaml}:/config/config.yaml:ro
      - ${SCORER_PROFILE_PATH:-./candidate-profile.md}:/config/candidate-profile.md:ro
