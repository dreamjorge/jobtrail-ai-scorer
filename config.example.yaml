# The canonical JobTrail AI scorer config example lives at
# `scripts/scorer-config.example.yaml`. Copy that file to a local, ignored
# `config.yaml` and edit only the local copy. Do not duplicate the YAML
# settings in this file — this pointer exists so the repo has a single
# source of truth for the example.
#
# See `docs/runtime-automation.md` for the runtime layout, the historical
# runtime-duplicate purge helper (`scripts/_purge_runtime_example.py --yes`),
# and the leak-detector test (`tests/test_example_redaction.py`) that fails
# CI if private IPv4 ranges, runtime paths, or credential prefixes ever creep
# back into committed examples.
